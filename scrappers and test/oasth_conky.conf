conky.config = {
    -- Position (Top Right is a good default)
    alignment = 'top_right',
    gap_x = 20,
    gap_y = 50,

    -- Window Settings (Standard transparent desktop setup)
    own_window = true,
    own_window_type = 'dock', -- Keeps it on desktop
    own_window_hints = 'undecorated,below,sticky,skip_taskbar,skip_pager',
    own_window_transparent = false,
    own_window_argb_visual = true,
    own_window_argb_value = 100, -- Dark glass effect
    own_window_colour = '#000000',
    double_buffer = true,

    -- Font & Color
    use_xft = true,
    font = 'Noto Sans:bold:size=10', -- Users should customize this for alignment
    default_color = 'white',
    
    -- Update Interval (in seconds)
    update_interval = 60,
}

conky.text = [[
${font Noto Sans:bold:size=14}${color #3daee9}BUS SCHEDULE (OASTH)${color}${font}
${voffset 5}${hr 2}
${voffset 5}
${font Noto Sans Mono:size=12}${execpi 60 /path/to/project/venv/bin/python3 /path/to/project/bus_timer.py}
]]